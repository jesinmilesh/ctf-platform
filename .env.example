# ==============================================================================
# XPLOITX // CYBER BATTLEFIELD PLATFORM CONFIGURATION
# ==============================================================================

# Core Application Environment
NODE_ENV=production
PORT=4000

# Authentication & Security
JWT_SECRET=super_secret_high_entropy_tactical_key_replace_in_prod_2026
JWT_EXPIRATION=24h

# Competition Standard Flag Syntax
FLAG_PREFIX=XploitXβ{
FLAG_SUFFIX=}

# PostgreSQL Authoritative Database
DB_TYPE=postgres
DATABASE_URL=postgresql://xploitx_admin:tactical_secret_db_pass_2026@localhost:5432/xploitx_db
POSTGRES_DB=xploitx_db
POSTGRES_USER=xploitx_admin
POSTGRES_PASSWORD=tactical_secret_db_pass_2026

# Redis Telemetry & EventBus
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# Tactical Sandbox Orchestration
SERVER_HOST=xploitxctf.me
INSTANCE_PORT_MIN=41000
INSTANCE_PORT_MAX=41999
CONTAINER_CPU_LIMIT=0.5
CONTAINER_MEM_LIMIT=256m
DEFAULT_INSTANCE_TTL_MINUTES=30

# File Storage
STORAGE_DIR=./challenge-storage
MAX_FILE_SIZE_BYTES=52428800 # 50 MB
